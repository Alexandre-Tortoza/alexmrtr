<header
  id="navbar"
  class="fixed top-0 left-0 right-0 z-50 flex -translate-y-full justify-center px-2 py-4"
>
  <nav id="navbar-container" class="flex w-full max-w-6xl items-center justify-between gap-4 rounded-xl border border-transparent px-6 py-2 transition-all duration-500">
    <a href="/" class="navbar-link group flex items-center gap-2 text-sm font-bold tracking-[0.3em] uppercase transition-all px-4 py-1.5 rounded-full border border-transparent">
      Alexandre Tortoza
    </a>
    <div id="navbar-links" class="flex items-center gap-3 text-[10px] tracking-widest uppercase sm:gap-5 sm:text-xs">
      <a href="/" class="navbar-link relative px-4 py-1.5 rounded-full border border-transparent transition-all">
        Início
        <span class="navbar-line absolute -bottom-1 left-1/2 h-px w-0 -translate-x-1/2 bg-mesh-1 transition-all" />
      </a>
      <a href="/projects" class="navbar-link relative px-4 py-1.5 rounded-full border border-transparent transition-all">
        Projetos
        <span class="navbar-line absolute -bottom-1 left-1/2 h-px w-0 -translate-x-1/2 bg-mesh-1 transition-all" />
      </a>
      <a href="/blog" class="navbar-link relative px-4 py-1.5 rounded-full border border-transparent transition-all">
        Blog
        <span class="navbar-line absolute -bottom-1 left-1/2 h-px w-0 -translate-x-1/2 bg-mesh-1 transition-all" />
      </a>
      <a href="/photos" class="navbar-link relative px-4 py-1.5 rounded-full border border-transparent transition-all">
        Fotos
        <span class="navbar-line absolute -bottom-1 left-1/2 h-px w-0 -translate-x-1/2 bg-mesh-1 transition-all" />
      </a>
    </div>
  </nav>
</header>

<script>
  import { initNavbar } from "./navbar.ts";

  const cleanup = initNavbar();

  document.addEventListener("astro:before-swap", () => {
    if (cleanup) cleanup();
  });
</script>
