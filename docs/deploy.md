# Deploy (Netlify)

- `pnpm build` generates `dist/`
- Netlify auto-detects Astro
- Redirects/Headers via `public/_redirects`
- Environment variables in Netlify Dashboard
- Auto-deploy on push to `main`
